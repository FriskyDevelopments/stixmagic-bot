name: Legacy Deploy Notice

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: |
          echo 'Automatic deploy-on-push has been retired.'
          echo 'Use CI for validation, Development Bot Readiness for dev checks, and Production Deployment Preparation before a manual production rollout.'
