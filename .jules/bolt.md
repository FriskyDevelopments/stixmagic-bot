## 2024-05-28 - 🧪 Add tests for ExportResult summary
**Learning:** Adding unit tests for small utility properties in dataclasses like `ExportResult` ensures that refactoring changes to the dataclass attributes are caught early. The `summary` property itself provides a readable string of successful and failed formats for debugging and logging.
**Action:** Implemented the `summary` property in `pipeline/exporters/__init__.py`'s `ExportResult` and added a comprehensive test suite in `tests/pipeline/exporters/test_export_result.py` that verifies the string generation for all states (all OK, FAILED, with errors).
